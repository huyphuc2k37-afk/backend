[phases.setup]
nixPkgs = ["nodejs_20", "openssl", "pkg-config"]

[phases.install]
cmds = ["npm ci --include=dev --ignore-scripts"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "node dist/index.js"
